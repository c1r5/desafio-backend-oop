describe('authorization test suite', () => {
    beforeAll(async () => {
    })
    afterAll(async () => {
    })

    it('should unauthorize a invalid session and response with 401', async () => {
    })
    it('should authorize a valid session and response with 200', async () => {
    })
    it('should avoid PJ users to use transfer endpoints', async () => {
    })
    it('should enable normal users to transfer', async () => {
    })
})